Manual test scripts and fixtures used during local API checking.

This folder is intentionally separate from the main application source so the
project root stays clean and it is easier to distinguish production code from
one-off debugging scripts.
